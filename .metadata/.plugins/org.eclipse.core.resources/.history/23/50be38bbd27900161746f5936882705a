package footbag;

public class Entity {
	private int x, y;
	private Map dots;
	
	public Entity(int x, int y, int w, int h){
		this.x = x;
		this.y = y;
		dots = new Map(w,h);
		
		dots.putDot(x, y, new Dot((byte)0, 1f));
		
	}
	
}
